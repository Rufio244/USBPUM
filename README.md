# USBPUM
# USBPUM
ระบบเรียนรู้แบบกระจายสำหรับ DeepSeek API

## ติดตั้ง
```bash
git clone https://github.com/thanwa/usbpum.git
cd usbpum
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env  # แก้ไขคีย์ในไฟล์
python main.py
